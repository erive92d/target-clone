export type ProductType = {
    category: string
    description: string
    id: number
    image: string
    price: string
    raing: {
        rate: number,
        count: number
    }
    title: string

}