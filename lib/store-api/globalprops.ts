export type ProductType = {
    category: string
    description: string
    id: number
    image: string
    price: number
    raing: {
        rate: number,
        count: number
    }
    title: string

}